# Orange Lab — Microbiology CDSS
# Minimal, default-light look (no custom theme). Fast & smooth.
# You can also simply DELETE this file to use Streamlit's plain defaults.
[theme]
base = "light"

[browser]
gatherUsageStats = false
